// NotificationService removed — notifications feature disabled per request.
// If you want to re-enable notifications later, recreate this service with
// appropriate platform implementations (flutter_local_notifications + timezone).

class NotificationService {
  NotificationService._();
  static final NotificationService instance = NotificationService._();
}
